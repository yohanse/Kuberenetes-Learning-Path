apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: quiz-data
spec:
  resources:
    requests:
      storage: 1Gi
  storageClassName: ""
  accessModes:
  - ReadWriteOnce
  volumeName: quiz-data